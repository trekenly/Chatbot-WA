{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://busx.example/schemas/TripChoiceRequest.schema.json",
  "title": "TripChoiceRequest",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "choice_index": {
      "type": "integer",
      "minimum": 1,
      "maximum": 10,
      "description": "User picks option 1-10 from displayed search results."
    },
    "fare_ref_id": {
      "type": "string",
      "minLength": 6,
      "description": "Preferred once known; matches BusX fare_ref_id."
    },
    "trip_id": {
      "type": "string",
      "description": "Optional (if returned by API); helpful for debugging."
    },
    "confirm": {
      "type": "boolean",
      "default": true,
      "description": "True when user confirms they want seat layout for this trip."
    }
  },
  "anyOf": [
    { "required": ["choice_index"] },
    { "required": ["fare_ref_id"] }
  ]
}
